import { AlertItDirective } from './alert-it.directive';

describe('AlertItDirective', () => {
  it('should create an instance', () => {
    const directive = new AlertItDirective();
    expect(directive).toBeTruthy();
  });
});
